BUA / GM 1227165 STEP 169 - 160-BAUD FACTORY MANAGER\n=======================================================\n\nStep 169 translates $F88E-$F8B4. Factory selectors 1, 2 and 3 choose the exact FC72, FC82 and FC92 pointer tables. Even indices dereference a word, send its first byte and retain its second at L017A; odd indices send that retained byte. Index 16 takes the common $FF reset. A zero factory selector preserves the listing's early exit. Strict C89 regression covers all three streams, paired-byte state, terminal reset and zero-selector behavior. Together Steps 168 and 169 close the bounded LF880 software gap identified by Step 167; electrical ALDL behavior remains a HAL boundary.\n